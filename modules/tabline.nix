_: {
  config.vim.tabline = {
    nvimBufferline = {
      enable = true;
      mappings = {
        closeCurrent = "<leader>bx";
      };
      setupOpts = {
        options = {
          numbers = "none";
        };
      };
    };
  };
}
